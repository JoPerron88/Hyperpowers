Ajoute une fonction `forEachAsync(items, fn)` dans `asyncUtils.js` qui exécute `fn` sur
chaque élément **en séquence** (l'un après l'autre) et ne se résout qu'une fois que tous
les appels sont terminés.

Mets à jour ou ajoute un test pour couvrir ce comportement.

Le code de départ est dans le dossier de travail courant.
