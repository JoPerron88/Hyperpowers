Écris une fonction `isEven(n)` qui retourne `true` si `n` est pair, `false` sinon.
Place-la dans `isEven.js`.

Ajoute un test pour couvrir ce comportement.

Le code de départ est dans le dossier de travail courant.
